apiVersion: v1
kind: Service
metadata:
  labels:
    app: scw-fe-test
  name: scw-fe-test
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: scw-fe-test
