apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: scw-fe-test-ingress
  annotations:
spec:
  rules:
    - host: localhost
      http:
        paths:
          - paths: /
            pathType: Prefix
            backend:
              service:
                name: scw-fe-test
                port:
                  number: 80
